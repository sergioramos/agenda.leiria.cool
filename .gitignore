# Python
__pycache__/
*.pyc
.venv/
venv/
env/
.pytest_cache/

# Node (site has no build, but just in case)
node_modules/

# Vercel CLI link metadata (created by `vercel link`)
.vercel

# Local secrets — NEVER commit. Keys live in GitHub Actions secrets / your browser.
.env
*.local
secrets.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Crawl working files
crawler/.cache/
_partials/
*.log

# Scratch / scraping experiments (never commit)
_sg*.html
_sg*.txt
*_shotgun_*.html

.vercel
